{
  "pluginAlias": "Arlo",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "<p align='center'><img width='250px' src='https://github.com/homebridge-plugins/homebridge-arlo/raw/master/logos/arlo-wordmark-logo-vertical.png'></p>\n\nThe **Homebridge Arlo** plugin allows you to control your Arlo base station.",
  "footerDisplay": "Please raise any issues on our [project page](https://github.com/homebridge-plugins/homebridge-arlo/issues).",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "Arlo"
      },
      "arloUser": {
        "title": "Arlo Username",
        "type": "string",
        "required": true,
        "placeholder": "email@email.com",
        "x-schema-form": {
          "type": "email"
        }
      },
      "arloPassword": {
        "title": "Password",
        "type": "string",
        "required": true,
        "placeholder": "Arlo password…",
        "x-schema-form": {
          "type": "password"
        }
      },
      "mfa": {
        "title": "Use MFA",
        "type": "boolean",
        "required": false,
        "description": "."
      },
      "emailUser": {
        "title": "Email User",
        "type": "string",
        "required": false,
        "placeholder": "mode#",
        "description": ".",
        "x-schema-form": {
          "type": "email"
        }
      },
      "emailPassword": {
        "title": "Email Password",
        "type": "string",
        "required": false,
        "placeholder": "mode#",
        "description": ".",
        "x-schema-form": {
          "type": "password"
        }
      },
      "emailServer": {
        "title": "Email User",
        "type": "string",
        "required": false,
        "placeholder": "mode#",
        "description": "The mode# label for the custom mode created in Arlo for the night state."
      },
      "refreshRate": {
        "title": "RefreshRate",
        "type": "string",
        "required": false,
        "placeholder": "mode#",
        "description": "The mode# label for the custom mode created in Arlo for the night state."
      },
      "logging": {
        "title": "Logging",
        "type": "string",
        "required": false,
        "placeholder": "mode#",
        "description": "The mode# label for the custom mode created in Arlo for the night state."
      }
    }
  },
  "layout": [
    "arloUser",
    "arloPassword",
    "mfa",
    "emailUser",
    "emailPassword",
    "emailServer",
    "refreshRate",
    "logging"
  ]
}
